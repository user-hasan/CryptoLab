# CryptoLab — التثبيت والتشغيل دون إنترنت

## الحزمة الجاهزة

تم بناء مُثبّت Windows ذاتي الاكتفاء للنسخة `1.1.0` بمعمارية `win-x64`:

`installer/Output/CryptoLab Setup 1.1.0 win-x64.exe`

يحتوي المُثبّت على ملفات التطبيق و.NET Runtime اللازمة محلياً، ولذلك لا يحتاج المستخدم إلى تنزيل .NET أو الاتصال بالإنترنت بعد تثبيت التطبيق.

## طريقة التثبيت

انسخ ملف `CryptoLab Setup 1.1.0 win-x64.exe` إلى جهاز Windows، ثم شغّله واتبع خطوات التثبيت. يمكن اختيار إنشاء اختصار في قائمة ابدأ وعلى سطح المكتب. بعد اكتمال التثبيت، شغّل التطبيق من الاختصار أو من `CryptoLab.exe` داخل مجلد التثبيت.

## التحقق من عدم الحاجة إلى الإنترنت

تضم الحزمة ملفات `coreclr.dll` و`hostfxr.dll` و`hostpolicy.dll` وملفات Runtime الأخرى داخل مجلد التطبيق. تم اختبار التثبيت بصمت في مجلد مؤقت، وتحقق وجود 488 ملفاً وتشغيل `CryptoLab.exe` بنجاح بعد التثبيت.

## ملاحظة المعمارية

هذه النسخة مخصصة لأجهزة Windows ذات معمارية `x64`، وهي المعمارية القياسية لمعظم أجهزة Windows الحديثة. لتشغيل التطبيق على Windows ARM64 أو Windows 32-bit، يلزم إنشاء حزمة منفصلة بالمعمارية المناسبة؛ لا ينبغي تثبيت نسخة `win-x64` على تلك الأجهزة.

## ملفات الإصدار

إلى جانب مُثبّت Setup، يوجد مجلد النشر غير المضغوط في `publish/CryptoLab-win-x64`، ويمكن استخدامه كنسخة محمولة عبر تشغيل `CryptoLab.exe` مباشرة، بشرط إبقاء جميع الملفات الموجودة في المجلد معاً.

## نتيجة الاختبار

نجح بناء النشر الذاتي باستخدام `dotnet publish`، ونجح بناء مُثبّت Inno Setup، ونجح التثبيت الصامت برمز خروج `0`. بعد التثبيت وُجد الملف التنفيذي وتم تشغيله بنجاح لمدة اختبارية قصيرة. لم يُنفذ نشر أو رفع خارجي للملفات.
